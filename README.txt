Recipe App

For working of this app we will need to install node and mongoDB in our machine. 
Then fire up the mongoDB database and run this program using "node app.js"

Then go to the webpage with address : "localhost:3000"
Explore accordingly.